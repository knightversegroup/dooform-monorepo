// Empty DTO — admin "list everything" endpoint takes no input but we keep a typed
// shape so the use-case signature stays aligned with the rest of the codebase.
export class ListAllAnnouncementsDto {}
