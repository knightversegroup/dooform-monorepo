export enum AuthProvider {
  EMAIL = 'email',
  LINE = 'line',
  GOOGLE = 'google',
}

export enum RoleName {
  ADMIN = 'admin',
  USER = 'user',
}

export enum QuotaTransactionType {
  ADD = 'add',
  USE = 'use',
  REFUND = 'refund',
  RESET = 'reset',
  SET = 'set',
}
