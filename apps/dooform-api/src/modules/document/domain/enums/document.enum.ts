export enum DocumentStatus {
  PROCESSING = 'PROCESSING',
  COMPLETED = 'COMPLETED',
  FAILED = 'FAILED',
}

export enum DocumentFormat {
  DOCX = 'docx',
  PDF = 'pdf',
}

export enum UserTier {
  FREE = 'free',
  PRO = 'pro',
  MAX = 'max',
}
